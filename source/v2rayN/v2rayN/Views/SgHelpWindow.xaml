<Window
    x:Class="v2rayN.Views.SgHelpWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
    Title="Справка SG Client"
    Width="974"
    Height="714"
    MinWidth="900"
    MinHeight="640"
    Background="{DynamicResource SgBackgroundBrush}"
    Foreground="{DynamicResource SgTextBrush}"
    WindowStartupLocation="CenterOwner"
    ResizeMode="CanResize"
    WindowStyle="None"
    SnapsToDevicePixels="True"
    UseLayoutRounding="True">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome
            CaptionHeight="0"
            CornerRadius="0"
            GlassFrameThickness="0"
            ResizeBorderThickness="7"
            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="HeaderActionButton" BasedOn="{StaticResource SgWindowButtonBase}" TargetType="Button">
            <Setter Property="Height" Value="34" />
            <Setter Property="MinWidth" Value="88" />
            <Setter Property="Margin" Value="6,0,0,0" />
            <Setter Property="Padding" Value="13,4" />
            <Setter Property="Background" Value="{DynamicResource SgNeutralActionBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource SgNeutralActionTextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource SgNeutralActionBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="MinHeight" Value="35" />
            <Setter Property="Margin" Value="0,1" />
            <Setter Property="Padding" Value="12,7" />
            <Setter Property="Foreground" Value="{DynamicResource SgMutedBrush}" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="ItemBorder" Padding="{TemplateBinding Padding}" Background="Transparent" BorderBrush="Transparent" BorderThickness="1" CornerRadius="9">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource SgHoverBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource SgTextBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource SgSelectedBrush}" />
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{DynamicResource SgAccentBorderBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource SgAccentBrush}" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource SgBackgroundBrush}" BorderBrush="{DynamicResource SgBorderStrongBrush}" BorderThickness="1" CornerRadius="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="62" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Padding="20,0,10,0" Background="{DynamicResource SgHeaderBrush}" BorderBrush="{DynamicResource SgBorderBrush}" BorderThickness="0,0,0,1" CornerRadius="15,15,0,0" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock FontSize="19" FontWeight="SemiBold" Foreground="{DynamicResource SgTextBrush}" Text="Справка SG Client" />
                        <TextBlock Margin="0,3,0,0" FontSize="10.5" Foreground="{DynamicResource SgMutedBrush}" Text="Полное руководство по режимам, маршрутизации, GeoFiles и экспертным настройкам" />
                    </StackPanel>
                    <Button Grid.Column="1" Style="{StaticResource HeaderActionButton}" Click="Close_Click" Content="Закрыть" />
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="250" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Background="{DynamicResource SgSidebarBrush}" BorderBrush="{DynamicResource SgBorderBrush}" BorderThickness="1" CornerRadius="14">
                    <ListBox x:Name="lstSections" SelectionChanged="Sections_SelectionChanged" Margin="7" Padding="0" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource SgTextBrush}" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListBoxItem Tag="start" Content="Быстрый старт" />
                            <ListBoxItem Tag="modes" Content="Режимы подключения" />
                            <ListBoxItem Tag="import" Content="Импорт профилей" />
                            <ListBoxItem Tag="subscriptions" Content="Подписки" />
                            <ListBoxItem Tag="profiles" Content="Профили и источники" />
                            <ListBoxItem Tag="routing" Content="Умная маршрутизация" />
                            <ListBoxItem Tag="connections" Content="Соединения и маршруты" />
                            <ListBoxItem Tag="geofiles" Content="GeoFiles и источники" />
                            <ListBoxItem Tag="dpi" Content="Маскировка DPI" />
                            <ListBoxItem Tag="dns" Content="DNS" />
                            <ListBoxItem Tag="killswitch" Content="Kill Switch" />
                            <ListBoxItem Tag="split" Content="Раздельный TUN" />
                            <ListBoxItem Tag="lan" Content="Локальная сеть" />
                            <ListBoxItem Tag="mtu" Content="MTU" />
                            <ListBoxItem Tag="stack" Content="Сетевой стек" />
                            <ListBoxItem Tag="reserve" Content="Резервный профиль" />
                            <ListBoxItem Tag="awg" Content="AmneziaWG" />
                            <ListBoxItem Tag="expert" Content="Экспертные настройки" />
                            <ListBoxItem Tag="expert-vless" Content="VLESS: raw и XHTTP" />
                            <ListBoxItem Tag="security" Content="REALITY, TLS и сертификаты" />
                            <ListBoxItem Tag="json" Content="JSON и проверка ядром" />
                            <ListBoxItem Tag="maintenance" Content="Обслуживание" />
                            <ListBoxItem Tag="updates" Content="Обновление ядер" />
                            <ListBoxItem Tag="backups" Content="Резервные копии" />
                            <ListBoxItem Tag="logs" Content="Журнал и диагностика" />
                    </ListBox>
                </Border>

                <Border Grid.Column="2" Padding="26,22" Background="{DynamicResource SgSurfaceBrush}" BorderBrush="{DynamicResource SgBorderBrush}" BorderThickness="1" CornerRadius="14">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel>
                            <TextBlock x:Name="txtTitle" FontSize="23" FontWeight="SemiBold" Foreground="{DynamicResource SgTextBrush}" TextWrapping="Wrap" />
                            <Border Margin="0,12,0,14" Height="1" Background="{DynamicResource SgBorderBrush}" />
                            <TextBlock x:Name="txtBody" FontSize="12" LineHeight="20" Foreground="{DynamicResource SgTextBrush}" TextWrapping="Wrap" />
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
